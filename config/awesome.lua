
-- TODO default config easy somehow! (fallback nicely)

local config = {
    meta = {
        providers = {
            LuaHot = {
                poll_rate = 2,
                -- enabled = true
            }
        }
    }
}

return config